{
  "name": "lexa.iot",
  "version": "0.1.0",
  "description": "IoT App",
  "application-type": "iot",
  "container-registry": "omborigridregistry.azurecr.io",
  "license": "UNLICENSED",
  "type": "module",
  "private": true,
  "eslintConfig": {
    "extends": "react-app"
  },
  "dependencies": {
    "@ombori/ga-module": "^2.50.4"
  },
  "devDependencies": {
    "@ombori/ts-schema": "^2.39.3",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "bootstrap": "echo init ok",
    "schema": "mkdir -p build && npx ts-schema src/schema.ts build/",
    "build": "mkdir -p build && tsc && yarn schema && touch build/index.html && omg module build && omg app build",
    "upload-description": "omg app upload-description"
  }
}
